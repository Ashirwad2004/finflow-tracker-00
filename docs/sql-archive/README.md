# SQL archive

Historical SQL scripts kept for reference. **Do not run these on production** if the equivalent change already exists in `supabase/migrations/`.

The live schema source of truth is `supabase/migrations/`.
